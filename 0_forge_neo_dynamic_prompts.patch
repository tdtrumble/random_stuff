# Instructions
#
# Save this the file to the root of your Forge repository
#
# Apply it with:
#
#   git apply 0_forge_neo_dynamic_prompts.patch
#
# If the patch applies successfully, git will produce no output.
# If it fails, ensure you're running the command from the repository root.
#
# To verify:
#   git diff
#
diff --git a/extensions/sd-dynamic-prompts/sd_dynamic_prompts/callbacks.py b/extensions/sd-dynamic-prompts/sd_dynamic_prompts/callbacks.py
index 1f54f29..557033c 100644
--- a/extensions/sd-dynamic-prompts/sd_dynamic_prompts/callbacks.py
+++ b/extensions/sd-dynamic-prompts/sd_dynamic_prompts/callbacks.py
@@ -5,8 +5,11 @@ from pathlib import Path
 from typing import Any
 
 from dynamicprompts.wildcards import WildcardManager
-from modules import script_callbacks
-from modules.generation_parameters_copypaste import parse_generation_parameters
+from modules import script_callbacks, shared
+try:
+    from modules import infotext_utils as generation_parameters_copypaste
+except ImportError:
+    from modules import generation_parameters_copypaste
 from modules.script_callbacks import ImageSaveParams
 
 from sd_dynamic_prompts.pnginfo_saver import strip_template_info
@@ -31,13 +34,13 @@ def register_on_infotext_pasted() -> None:
         new_parameters = {}
         if "Prompt" in parameters and "Template:" in parameters["Prompt"]:
             strip_template_info(parameters)
-            new_parameters = parse_generation_parameters(parameters["Prompt"])
+            new_parameters = generation_parameters_copypaste.parse_generation_parameters(parameters["Prompt"])
         elif (
             "Negative prompt" in parameters
             and "Template:" in parameters["Negative prompt"]
         ):
             strip_template_info(parameters)
-            new_parameters = parse_generation_parameters(parameters["Negative prompt"])
+            new_parameters = generation_parameters_copypaste.parse_generation_parameters(parameters["Negative prompt"])
             new_parameters["Negative prompt"] = new_parameters["Prompt"]
             new_parameters["Prompt"] = parameters["Prompt"]
         parameters.update(new_parameters)
